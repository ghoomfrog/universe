# Bunch

A bunch is a structure where an object $O$ exists $n$ times where $n$ is a real number. It is written $\_nE$, which, if $n$ is an integer, is equivalent to $\set{E, E, ...}$ where there are $n$ instances of $E$.
